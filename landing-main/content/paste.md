---
title: "Paste"
description: "Поделитесь кодом, логом или конфигом короткой ссылкой – без регистрации и на 30 дней"
layout: "paste"
aliases:
  - /pastebin/
  # Bare /p/ only. nginx proxies /p/<id> to the backend via a regex that requires
  # at least one id char, so this redirect never shadows a real paste.
  - /p/
---
