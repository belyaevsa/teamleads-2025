# Год в Обзоре: Тимлид не кодит 2025

Комплексный анализ сообщества тимлидов Казахстана за 2025 год.

## 📁 Структура Репозитория

```
teamleads-2025/
├── scripts/                  # Python скрипты анализа
│   ├── analyze_better.py
│   ├── analyze_contextual.py
│   ├── analyze_sentiment_rubert.py
│   ├── network_analysis.py
│   └── ...
├── data/                     # CSV данные и результаты
│   ├── messages_export.csv
│   ├── sentiment_by_month_corrected.csv
│   ├── network_user_stats.csv
│   └── ...
├── hugo-claude/              # Веб-сайт (Hugo)
│   ├── content/
│   ├── layouts/
│   └── package.json
├── year-in-review-2025.md   # Главный отчет
├── methodology.md            # Методология анализа
├── network-analysis-report.md
├── topic-implications-report.md
└── README.md                 # Этот файл
```

---

## 📚 Навигация по Документам

### 🎯 Главный Отчет

**[year-in-review-2025.md](year-in-review-2025.md)** - Основной годовой отчет

Содержит:
- Общую статистику сообщества
- Героев и активных участников
- Месяц за месяцем историю
- Топ-обсуждения и темы
- Анализ настроений
- Сетевой анализ взаимодействий
- Культуру и тренды

**Размер**: ~1,000 строк
**Формат**: Markdown с таблицами, графиками и ссылками
**Аудитория**: Участники чата, новички, аналитики

### 🌐 Веб-версия

**[hugo-claude/](hugo-claude/)** - Интерактивный веб-сайт на Hugo

Красивая веб-версия годового отчета с анимациями и интерактивными элементами.

**Возможности:**
- Анимированные метрики с count-up эффектами
- Glassmorphism дизайн с Aurora градиентами
- Адаптивная навигация (floating dock в стиле macOS)
- Секции: Герои сообщества, Топ темы, Сетевой анализ
- Полная локализация на русский язык

**Технологии:**
- Hugo v0.140.0 (статический генератор сайтов)
- TailwindCSS v3.4 (CSS фреймворк)
- Alpine.js v3 (интерактивность)
- PostCSS

**Как запустить:**
```bash
cd hugo-claude
npm install           # Установить зависимости
npm run dev          # Запустить dev-сервер
# Открыть http://localhost:1313
```

**Как собрать для продакшена:**
```bash
cd hugo-claude
npm run build        # Собрать сайт
# Результат в папке public/
```

**Подробности**: См. [hugo-claude/README.md](hugo-claude/README.md)

---

## 🔬 Детальные Аналитические Отчеты

### 1. [topic-implications-report.md](topic-implications-report.md)

**Анализ распределения главных тем дискуссий**

**Что внутри:**
- Распределение 4 главных категорий (Технологии, Люди, Бизнес, Менеджмент)
- Детализация по подтемам (AI/ML, Найм джунов, Метрики, etc.)
- Пересечения тем (co-occurrence analysis)
- Эволюция тем по кварталам
- Ключевые импликации для каждой категории

**Ключевые находки:**
- AI/ML = 59.2% tech-дискуссий (доминирование)
- Security = 5.7% (критический разрыв!)
- Найм джунов = 21.4% people-тем (дефицит кадров)
- Метрики = 42.6% management-тем (нерешенная проблема)

**Размер**: 600 строк
**Метод**: Keyword matching с категоризацией

---

### 2. [contextual-topic-analysis.md](contextual-topic-analysis.md)

**Контекстный анализ тем по дням**

**Что внутри:**
- 10 детальных тем (Управление командой, Процессы, AI/ML, Тестирование, etc.)
- Темы по дням (295 дней активности)
- Топ-дни для каждой темы с примерами сообщений
- Сезонные паттерны
- Эволюция тем в течение года

**Метод:**
- Контекстный анализ (5 сообщений до/после + тред)
- Weighted scoring (само сообщение 3x, контекст 1x)
- 39,111 упоминаний тем

**Ключевые находки:**
- Управление командой = 22.4% (главная тема тимлидов)
- Конфликты = 21.0% (открытая культура)
- Май 2025 - пик обсуждения управления (1,025 сообщений)

**Размер**: 400 строк
**Генерируемые файлы**:
- `daily_topics.csv` (9,433 строки)
- `message_topics.csv` (23,426 строк)
- `management_days.csv`

---

### 3. [network-analysis-report.md](network-analysis-report.md)

**Анализ социальной сети чата**

**Что внутри:**
- Структура взаимодействий (кто с кем общается)
- "Большая четверка" центрального ядра
- Профили ключевых участников
- Топ-10 сильнейших связей
- Временные паттерны (когда общаемся)
- Риски и рекомендации

**Метод:**
- Graph theory (reply network)
- Temporal proximity analysis (5-минутное окно)
- Centrality metrics (degree, betweenness)
- Роли: Хабы, Влиятели, Коннекторы, Усилители

**Ключевые находки:**
- 4 человека = 56% всех взаимодействий
- Andrii Kurdiumov - суперхаб (124 связи, 2,047 ответов)
- Stanislav Belyaev - главный коннектор (129 связей)
- Артур Пан - главный влиятель (26.4 engagement rate)
- Пик активности: 11:00-12:00 (Almaty time)

**Размер**: 700 строк
**Генерируемые файлы**:
- `network_reply_edges.csv` (~10,000 связей)
- `network_temporal_edges.csv`
- `network_user_stats.csv` (141 участник)
- `network_mutual_connections.csv`

---

### 4. [sentiment-analysis-results.md](sentiment-analysis-results.md)

**Анализ настроений и тональности**

**Что внутри:**
- Общее распределение настроений
- Sentiment по месяцам
- Sentiment по темам
- Здоровье сообщества

**Метод:**
- Keyword-based sentiment analysis
- Improved question detection
- Monthly aggregation

**Ключевые находки:**
- 69.6% нейтральных (профессиональная атмосфера)
- 11.8% вопросов (культура обучения)
- 10.0% негативных (проблемы обсуждаются)
- 8.6% позитивных
- Эволюция: от 66% нейтральных (Q1) к 70% (Q4) — сообщество становится спокойнее

**Размер**: 220 строк

---

## 🛠️ Методология и Скрипты

### [methodology.md](methodology.md)

**Полное описание методологии создания отчета**

**Что внутри:**
- Источник данных (PostgreSQL schema)
- Этапы анализа (6 этапов)
- Инструменты и скрипты (все Python файлы)
- Алгоритмы и методы (детально)
- Ограничения и допущения
- Воспроизводимость

**Для кого:**
- Исследователей, желающих повторить анализ
- Разработчиков, создающих похожие отчеты
- Студентов, изучающих data analysis
- Любопытных участников чата

**Размер**: 1,200 строк
**Формат**: Образовательный, с примерами кода

---

## 🐍 Python Скрипты

Все скрипты находятся в папке **`scripts/`** и написаны на **Python 3.x** с использованием **только стандартной библиотеки** (без внешних зависимостей).

### 1. `scripts/analyze_simple.py`

**Первичный анализ тональности** (устаревший, содержит ошибку)

- Простой keyword matching
- Проблема: 49.9% вопросов (неправильно!)
- Использовался для первых выводов

**Статус**: Сохранен для истории, не рекомендуется использовать

---

### 2. `scripts/analyze_better.py`

**Улучшенный анализ тональности**

**Что делает:**
- Исправленное определение вопросов
- Более точный sentiment analysis
- Агрегация по месяцам

**Алгоритм:**
```python
def is_question(text):
    # 1. Заканчивается на '?'
    if text.strip().endswith('?'):
        return True

    # 2. Начинается с вопросительного слова
    if text.lower().startswith(('как ', 'что ', 'где ', ...)):
        return True

    # 3. Просьбы о помощи
    if any(word in text.lower() for word in ['подскажите', 'помогите', ...]):
        return True

    return False
```

**Входные данные**: `data/messages_export.csv`
**Выходные данные**: `data/sentiment_by_month_corrected.csv`

**Запуск**:
```bash
python3 scripts/analyze_better.py
```

**Результат**: 11.8% вопросов (правильно!)

---

### 3. `scripts/analyze_contextual.py`

**Контекстный анализ тем**

**Что делает:**
- Строит граф ответов (threads)
- Получает контекст сообщения (5 до/после + тред)
- Определяет темы с weighted scoring
- Генерирует daily topic breakdown

**Алгоритм:**
```python
def get_message_context(msg, window=5):
    # Получить 5 сообщений до и после в тот же день
    context_before = get_n_before(msg, n=5, same_day=True)
    context_after = get_n_after(msg, n=5, same_day=True)

    # Получить весь тред
    thread_context = get_thread_messages(msg)

    return context_before, context_after, thread_context

def analyze_topic(msg, ctx_before, ctx_after, thread):
    msg_text = msg['text']
    context_text = ' '.join([m['text'] for m in ctx_before + ctx_after + thread])

    scores = {}
    for topic, keywords in TOPICS.items():
        score = 0
        for kw in keywords:
            score += msg_text.count(kw) * 3  # Вес сообщения
            score += context_text.count(kw) * 1  # Вес контекста

        if score >= 2:
            scores[topic] = score

    return scores
```

**Темы:** 10 (Управление, Процессы, AI/ML, Тестирование, HR, Архитектура, Конфликты, Рост, Метрики, Удаленка)

**Входные данные**: `data/messages_export.csv`
**Выходные данные**:
- `data/daily_topics.csv` (темы × дни)
- `data/message_topics.csv` (темы каждого сообщения)
- `data/management_days.csv`

**Запуск**:
```bash
python3 scripts/analyze_contextual.py
```

**Время**: ~30 секунд на 23K сообщений

---

### 4. `scripts/topic-deep-dive-analysis.py`

**Детальный анализ категорий**

**Что делает:**
- Анализирует 4 главные категории из year-in-review
- Определяет подтемы (17 подтем)
- Находит пересечения тем (co-occurrence)
- Генерирует импликации и выводы

**Категории:**
- Менеджмент и Процессы
- Люди и Команды
- Технологии
- Бизнес

**Алгоритм:**
```python
MAIN_TOPICS = {
    'Технологии': ['ai', 'ml', 'chatgpt', 'архитектур', ...],
    'Люди и Команды': ['найм', 'джун', 'зарплат', 'конфликт', ...],
    # ...
}

SUB_TOPICS = {
    'AI/ML': ['ai', 'ml', 'chatgpt', 'gpt', 'llm', ...],
    'Найм джунов': ['найм', 'hiring', 'джун', 'junior', ...],
    # ...
}

# Match topics
for msg in messages:
    main = match_topics(msg, MAIN_TOPICS)
    sub = match_topics(msg, SUB_TOPICS)

    # Track combinations
    if len(main) > 1:
        combinations[tuple(sorted(main))] += 1
```

**Входные данные**: `data/messages_export.csv`
**Выходные данные**:
- `data/main_topics_distribution.csv`
- `data/sub_topics_distribution.csv`
- `data/topic_combinations.csv`

**Запуск**:
```bash
python3 scripts/topic-deep-dive-analysis.py
```

---

### 5. `scripts/network_analysis.py`

**Анализ социальной сети**

**Что делает:**
- Строит reply network (кто кому отвечает)
- Анализирует temporal proximity (кто пишет в одно время)
- Вычисляет метрики центральности
- Определяет роли участников
- Находит ключевые связи

**Алгоритм:**

```python
# 1. Построить reply network
reply_network = defaultdict(Counter)
for msg in messages:
    if msg['reply_to_messageid']:
        user = msg['user_name']
        replied_to = get_original_user(msg['reply_to_messageid'])
        reply_network[user][replied_to] += 1

# 2. Temporal proximity (5-минутное окно)
TIME_WINDOW = timedelta(minutes=5)
temporal_proximity = defaultdict(Counter)

for i in range(len(messages) - 1):
    if messages[i+1]['timestamp'] - messages[i]['timestamp'] <= TIME_WINDOW:
        user1 = messages[i]['user_name']
        user2 = messages[i+1]['user_name']
        if user1 != user2:
            temporal_proximity[user1][user2] += 1

# 3. Метрики
degree = count_unique_connections(user)
in_degree = count_incoming_connections(user)
out_degree = count_outgoing_connections(user)
connector_score = (in_degree + out_degree) * (in_volume + out_volume)

# 4. Роли
if degree > 100 and volume > 2000:
    role = 'Hub'
elif in_degree / out_degree > 1.2:
    role = 'Influencer'
elif out_degree / in_degree > 1.2:
    role = 'Amplifier'
# ...
```

**Входные данные**: `data/messages_export.csv`
**Выходные данные**:
- `data/network_reply_edges.csv` (все связи)
- `data/network_temporal_edges.csv` (временная близость)
- `data/network_user_stats.csv` (статистика участников)
- `data/network_mutual_connections.csv` (взаимные связи)

**Запуск**:
```bash
python3 scripts/network_analysis.py
```

**Время**: ~1 минута на 23K сообщений

---

### 6. `scripts/validate_sentiment_5k.py`

**Валидация sentiment analysis на большой выборке**

**Что делает:**
- Случайная выборка 5,000 сообщений (seed=42 для воспроизводимости)
- Запуск sentiment analysis на выборке
- Подробная статистика с доверительными интервалами
- Примеры сообщений для каждой категории
- Экспорт данных для ручной валидации

**Алгоритм:**
```python
# 1. Случайная выборка
random.seed(42)
sampled = random.sample(messages, 5000)

# 2. Анализ с подсчетом статистики
for msg in sampled:
    sentiment = analyze_sentiment(msg['text'])
    sentiment_dist[sentiment] += 1
    # Сохранить примеры
    if len(examples[sentiment]) < 20:
        examples[sentiment].append(msg)

# 3. Доверительные интервалы (95% CI)
p = count / total
se = sqrt(p * (1 - p) / total)
margin = 1.96 * se
ci_lower, ci_upper = p - margin, p + margin

# 4. Экспорт для ручной проверки
export_to_csv('validation_sample_5k.csv', sampled)
```

**Входные данные**: `data/messages_export.csv`

**Выходные данные**:
- `data/validation_sample_5k.csv` - 5,000 сообщений для ручной валидации
- `data/validation_statistics_5k.csv` - статистика с доверительными интервалами

**Запуск**:
```bash
python3 scripts/validate_sentiment_5k.py
```

**Результаты** (5,000 сообщений, 21.3% выборка):
- Нейтральные: 67.90% ± 1.29%
- Вопросы: 12.18% ± 0.91%
- Позитивные: 8.36% ± 0.77%
- Негативные: 11.56% ± 0.89%

**Время**: ~5 секунд

---

### 7. `scripts/analyze_sentiment_rubert.py`

**ML-based sentiment analysis с RuBERT** (опциональный, требует dependencies)

**Что делает:**
- Использует нейросеть (RuBERT) для sentiment analysis
- Более точное понимание контекста, сарказма, иронии
- Предоставляет confidence scores для каждого предсказания
- Экспортирует low-confidence predictions для ручной проверки

**Требования:**
```bash
python3 -m venv venv_ml
source venv_ml/bin/activate
pip install transformers torch
```

**Алгоритм:**
```python
# 1. Загрузить RuBERT модель
from transformers import AutoTokenizer, AutoModelForSequenceClassification
model = AutoModelForSequenceClassification.from_pretrained(
    "blanchefort/rubert-base-cased-sentiment-rurewiews"
)

# 2. Вопросы определять rule-based (как в keyword-based)
if is_question(text):
    return 'question', 1.0

# 3. Для остальных использовать ML
inputs = tokenizer(text, return_tensors='pt', truncation=True)
outputs = model(**inputs)
probs = F.softmax(outputs.logits, dim=-1)
sentiment = model.config.id2label[torch.argmax(probs).item()]
confidence = probs[0][predicted_class].item()

return sentiment, confidence
```

**Входные данные**: `data/messages_export.csv`

**Выходные данные**:
- `data/sentiment_by_month_rubert.csv` - разбивка по месяцам
- `data/sentiment_rubert_detailed.csv` - все сообщения с confidence scores
- `data/sentiment_rubert_low_confidence.csv` - предсказания с низкой уверенностью (<60%)

**Запуск**:
```bash
source venv_ml/bin/activate
python3 scripts/analyze_sentiment_rubert.py
```

**Результаты** (23,426 сообщений):
- Нейтральные: 69.0% (avg confidence: 65.1%)
- Вопросы: 11.8% (rule-based)
- Негативные: 12.5% (avg confidence: 60.0% ⚠️ низкая)
- Позитивные: 6.7% (avg confidence: 71.8%)

**Время**: ~5.5 минут (~14ms/message)

**Сравнение с keyword-based**: См. [sentiment-comparison-ml-vs-keywords.md](sentiment-comparison-ml-vs-keywords.md)

---

## 📊 CSV Файлы (Данные)

Все данные находятся в папке **`data/`**.

### Исходные данные

**`data/messages_export.csv`** (23,426 строк)
```csv
messageid,created_at,month,user_name,user_message,reply_to_messageid,msg_length
-1002424547330_123,2025-01-20 10:30:00,2025-01,Andrii,Как вы думаете...,,-
```

### Выходные данные

**Sentiment Analysis:**
- `data/sentiment_by_month.csv` - первая версия (устаревшая)
- `data/sentiment_by_month_corrected.csv` - исправленная версия (keyword-based)
- `data/validation_sample_5k.csv` - 5,000 сообщений для ручной валидации
- `data/validation_statistics_5k.csv` - статистика с доверительными интервалами
- `data/sentiment_by_month_rubert.csv` - RuBERT ML-based результаты по месяцам
- `data/sentiment_rubert_detailed.csv` - все сообщения с confidence scores (23,426 строк)
- `data/sentiment_rubert_low_confidence.csv` - низкая уверенность ML для проверки (7,530 строк)

**Topic Analysis:**
- `data/daily_topics.csv` - темы по дням (9,433 строки)
- `data/message_topics.csv` - темы каждого сообщения (23,426 строк)
- `data/management_days.csv` - дни управления/процессов
- `data/main_topics_distribution.csv` - главные категории
- `data/sub_topics_distribution.csv` - подтемы
- `data/topic_combinations.csv` - пересечения тем

**Network Analysis:**
- `data/network_reply_edges.csv` - все связи ответов (~10,000 строк)
- `data/network_temporal_edges.csv` - временная близость
- `data/network_user_stats.csv` - статистика участников (141 строка)
- `data/network_mutual_connections.csv` - взаимные связи

---

## 🚀 Как Воспроизвести Анализ

### Требования

- Python 3.8+
- PostgreSQL client (psql)
- Доступ к базе данных (read-only)

### Шаг 2: Запустить все скрипты

```bash
# 1. Sentiment analysis
python3 scripts/analyze_better.py
# → data/sentiment_by_month_corrected.csv

# 2. Contextual topic analysis
python3 scripts/analyze_contextual.py
# → data/daily_topics.csv, data/message_topics.csv, data/management_days.csv

# 3. Topic deep dive
python3 scripts/topic-deep-dive-analysis.py
# → data/main_topics_distribution.csv, data/sub_topics_distribution.csv, data/topic_combinations.csv

# 4. Network analysis
python3 scripts/network_analysis.py
# → data/network_*.csv (4 файла)
```

### Шаг 3: Проверить результаты

```bash
# Проверить количество строк
wc -l data/messages_export.csv
# Ожидается: 23,427 (23,426 + header)

wc -l data/daily_topics.csv
# Ожидается: ~9,433

wc -l data/network_reply_edges.csv
# Ожидается: ~10,000+
```

---

## 📈 Ключевые Метрики

**Данные:**
- 23,426 сообщений
- 141 участник
- 295 активных дней
- 9,848 цепочек обсуждений

**Темы:**
- 39,111 упоминаний тем
- 10 основных тем
- 4 главные категории
- 17 подтем

**Сеть:**
- ~10,000 связей ответов
- 4 центральных хаба
- 56% взаимодействий через ядро
- 129 максимальных связей (Stanislav)

**Sentiment:**
- 69.6% нейтральных
- 11.8% вопросов
- 10.0% негативных
- 8.6% позитивных

---

## 🎯 Использование Отчетов

### Для участников чата:

1. **Узнать о сообществе** - прочитать [year-in-review-2025.md](year-in-review-2025.md)
2. **Понять темы** - изучить [topic-implications-report.md](topic-implications-report.md)
3. **Увидеть связи** - посмотреть [network-analysis-report.md](network-analysis-report.md)

### Для исследователей:

1. **Понять методологию** - прочитать [methodology.md](methodology.md)
2. **Изучить скрипты** - разобрать Python файлы
3. **Воспроизвести** - повторить анализ на своих данных

### Для разработчиков:

1. **Адаптировать скрипты** для своего чата
2. **Улучшить алгоритмы** (добавить ML, NLP)
3. **Создать визуализации** (графы, дашборды)

---

## 🔮 Будущие Улучшения

**Планируется:**
- [ ] Визуализация графов (NetworkX, Gephi)
- [ ] ML-based sentiment analysis (ruBERT)
- [ ] Topic modeling (LDA, NMF)
- [ ] Temporal network analysis
- [ ] Interactive dashboard (Streamlit, Dash)
- [ ] Automated monthly reports

---

## 📞 Контакты

**Чат**: [@teamleads_kz](https://t.me/teamleads_kz)
**Автор анализа**: Claude Sonnet 4.5 (при поддержке Stanislav Belyaev)
**Дата**: Декабрь 2025

---

## 📄 Лицензия

Все материалы предоставлены для образовательных целей.
Скрипты - MIT License.
Данные - принадлежат участникам чата.

---

**Спасибо за интерес к нашему сообществу! 🚀**
